# Dependencies
node_modules/
vendor/

# Build
dist/
dist-ssr/
build/

# cPanel one-folder upload bundle (generated by npm run pack:cpanel)
deploy/cpanel/out/

# Environment
.env
.env.*
!.env.example
deploy.env

# Local / server-only PHP overrides (DB passwords, etc.)
backend/config.local.php
tunnel.env

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Uploaded assets (generated at runtime)
backend/public/uploads/

# IDE
.idea/
*.sw?
# SFTP (Cursor/VS Code “SFTP” extension) — real host/user/password; never commit
.vscode/sftp.json

# Local deployment bundles (generated; not for Git)
backend.zip
frontend/dist.zip
deploy.zip
